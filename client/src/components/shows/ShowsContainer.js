import React, { Component } from "react";
import PropTypes from "prop-types";

export default class ShowsContainer extends Component {
  static propTypes = {
    prop: PropTypes
  };

  render() {
    return <div>Hello from Shows!</div>;
  }
}
